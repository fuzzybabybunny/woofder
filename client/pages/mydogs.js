var findPetById = function(id){	return Adoptees.findOne(id);}var petsArray = [];petImagesArray = [];var petsSelectedArray = [];Template.myDogs.helpers({  adoptees: function() {  	if( Meteor.user() ){  		console.log("Meteor.user exists!");  		petsIdsArray = Meteor.user().profile.petsSelected;  		console.log("petsIdsArray:");  		console.log(petsIdsArray);	  	var petObject = {};	    for (var i = 0; i < petsIdsArray.length; i++) {	    	//grabs the petObject via the id	    	var petId = petsIdsArray[i];	    	console.log("Inside loop, petId:");	    	console.log(petId);	    	petObject = findPetById(petId);	    	console.log("Inside loop, petObject:");	    	console.log(petObject);	    	petObject.imageUrls = [];	    	var imageId = petObject.imageIds[0];	    	var imageUrl = AdopteeImages.findOne(imageId).url();	    	console.log("Inside loop, imageUrl:");	    	console.log(imageUrl);	    	petObject.imageUrl = imageUrl;	    	console.log("Inside loop, modified petObject:");	    	console.log(petObject);	    // 	//loops through the imageIds array of the petObject	    // 	for (var i = 0; i < petObject.imageIds.length; i++) {	    // 		//grabs the imageId	    // 		var imageId = petObject.imageIds[i]	    // 		//queries Mongo for the URL of that imageId			  // 	var imageURL = AdopteeImages.findOne(imageId).url();			  // 	//adds the imageUrl to the petObject.imageUrls array			  //   petObject.imageUrls.push(imageURL);			  //   console.log(petObject);		  	// }				// petObject.push( findPetById(petsSelectedArray[i]) );				petsArray.push(petObject);				console.log("New Pets Array:");			  console.log(petsArray);			};			return petsArray;  	}  },	'petImagesArray': function(){		var imageURL = "";	  for (var i = 0; i < petsIdsArray.length; i++) {	  	console.log(AdopteeImages.findOne(imageIdsArray[i]).url());	  	imageURL = AdopteeImages.findOne(imageIdsArray[i]).url();	    petImagesArray.push(imageURL);	    console.log(petImagesArray);	  }	  return petImagesArray;	}});